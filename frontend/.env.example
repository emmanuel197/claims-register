# Base URL of the Django API, no trailing slash. Leave unset in development:
# Vite proxies /api to http://localhost:8000 (see vite.config.js).
# VITE_API_BASE_URL=https://claims-register-api.onrender.com/api

